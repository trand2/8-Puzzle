# 8-Puzzle-Uninformed-Search
How to run this program:

1. From the command line run the following command:
`java -jar 8-Puzzle.jar`

2. Follow the commands that prompt on the screen to start the game.